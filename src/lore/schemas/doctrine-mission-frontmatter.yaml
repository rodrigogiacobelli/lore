$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "lore://schemas/doctrine-mission-frontmatter"
title: Doctrine Mission Frontmatter
type: object
additionalProperties: false
required: [id, title, summary]
properties:
  id:
    type: string
    minLength: 1
    description: Doctrine mission id. Must match the filename stem.
  title:
    type: string
    minLength: 1
  summary:
    type: string
    minLength: 1
