---
id: example-tech-frontend
title: Frontend
related: []
stability: stable
summary: >
  Frontend technology, project structure, routing, component conventions,
  and state management. Start here before adding or modifying any UI code.
---

# Frontend

_One paragraph. What kind of frontend is this (web app, CLI output, mobile)? What framework? What is the entry point?_

> If this project has no web frontend (e.g. it is a CLI tool), replace this file's content with a single sentence: "This project has no web frontend." Retain the file as a placeholder for teams that add a UI later.

## Framework and Rendering Strategy

_Framework (e.g. React, Vue, Svelte), rendering strategy (CSR, SSR, SSG), and key configuration._

## Project Structure

```
{frontend-dir}/
├── {entry-point}      # _description_
├── {pages-or-routes}/ # _description_
├── {components}/      # _description_
└── {assets}/          # _description_
```

## Routing

_How are routes defined? File-based or config-based? What are the naming conventions?_

## Component Conventions

_File naming, directory nesting rules, when to split a component, how props are typed._

## State Management

_What manages shared state (e.g. Zustand, Redux, Context API)? What belongs in global state vs local state?_
